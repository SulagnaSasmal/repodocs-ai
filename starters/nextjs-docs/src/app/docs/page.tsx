export default function DocsPage() {
  return (
    <main>
      <h1>Docs Workflow</h1>
      <p>Generate or draft docs with RepoDocs AI assets, validate them, then render approved content inside the site.</p>
    </main>
  );
}